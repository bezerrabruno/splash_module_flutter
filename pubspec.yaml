name: splash_module_flutter
description: Modulo Splash generico.
version: 1.0.0
homepage: none

environment:
  sdk: '>=3.2.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_modular: ^6.3.2
  flutter_svg: ^2.0.8
  
dev_dependencies:
  flutter_test:
    sdk: flutter
    
  flutter_lints: ^2.0.3

flutter:
